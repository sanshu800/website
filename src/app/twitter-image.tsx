/**
 * X/Twitter card. Same artwork as the Open Graph card, kept as its own route so
 * the two can diverge later without touching the shared design.
 */
export { default, alt, size, contentType } from "./opengraph-image";
